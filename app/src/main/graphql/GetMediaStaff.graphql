query GetMediaStaff($id: Int, $page: Int, $perPage: Int, $sort: [StaffSort]) {
    Media(id: $id) {
        staff(sort: $sort, page: $page, perPage: $perPage) {
            pageInfo {
                currentPage
                hasNextPage
            }
            edges {
                role
                node {
                    id
                    name {
                        userPreferred
                        full
                        native
                    }
                    image {
                        large
                    }
                    primaryOccupations
                }
            }
        }
    }
}
